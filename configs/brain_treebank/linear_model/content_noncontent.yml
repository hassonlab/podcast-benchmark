config_setter_name: set_input_channels
run_mode: per_subject
task_config:
  task_name: content_noncontent_task
  data_params:
    dataset_name: brain_treebank
    dataset_params:
      movie: cars-2
    data_root: data/brain-treebank
    subject_ids: [3, 7, 10]
    use_high_gamma: true
    target_sr: 512
    window_width: 1.0
    preprocessing_fn_name: window_average_neural_data
    preprocessor_params:
      num_average_samples: 32
  task_specific_config:
    labels_path: processed_data/brain_treebank/cars-2/word_labels.csv
training_params:
  tensorboard_logging: false
  batch_size: 512
  epochs: 100
  learning_rate: 0.05
  weight_decay: 0.001
  early_stopping_patience: 10
  n_folds: 5
  min_lag: -1000
  max_lag: 1100
  lag_step_size: 100
  losses: [bce]
  metrics: [roc_auc, f1, acc]
  early_stopping_metric: roc_auc
  smaller_is_better: false
trial_name: brain_treebank_cars2_linear_content_noncontent_per_subject
model_spec:
  constructor_name: torch_linear_model
  params:
    output_dim: 1
    bias: true
    activation: sigmoid

