model_constructor_name: foundation_model_finetune_attention
config_setter_name: foundation_model_finetune_mlp
model_params: 
  mlp_layer_sizes: [50]
  freeze_foundation_model: True
  num_unfrozen_blocks: 1
  model_dir: /scratch/gpfs/zparis/ECoG-foundation-model/checkpoints/grad_accum=2-encoder_mask_ratio=0.5-weight_decay=0.01-proj_drop=0.1-drop_path=0.05-lr=5e-05-precision=no-frame_patch_size=16_29112516/best_checkpoint
training_params:
  batch_size: 32
  epochs: 100
  learning_rate: 0.001
  weight_decay: 0.0001
  early_stopping_patience: 10
  n_folds: 5
  min_lag: -600
  max_lag: 1000
  lag_step_size: 200
data_params:
  # Cross-model fields
  data_root: data
  embedding_type: gpt-2xl
  embedding_layer: 24
  embedding_pca_dim: 50
  # Window width is set by config setter function to the model's sample length.
  window_width: -1
  preprocessing_fn_name: foundation_model_finetune_mlp
  subject_ids: [9]
  channel_reg_ex: ^G([1-9]|[1-5][0-9]|6[0-4])$
  # Model specific config.
  # preprocessor_params:
trial_name: foundation_finetune_improved_model_attention_frozen_num_unfrozen_blocks={}_dim={}_framepatchsize={}_patchsize={}
format_fields: [
    model_params.num_unfrozen_blocks,
    model_params.foundation_model_config.video_mae_task_config.vit_config.dim,
    model_params.foundation_model_config.video_mae_task_config.vit_config.frame_patch_size,
    model_params.foundation_model_config.video_mae_task_config.vit_config.patch_size,
  ]
